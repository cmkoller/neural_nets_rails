<% @neural_net.num_layers.times do |layer| %>

  <!-- Print Layer -->
  <div class="row layer layer_<%= layer %>">
    <%= render partial: 'neural_nets/layer', locals: {layer: layer} %>
  </div>

<% end %>
