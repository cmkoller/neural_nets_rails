<!-- MUST be provided a layer variable "layer"! -->


<%= form_for([@neural_net, @neural_net.nodes.new]) do |f| %>
  <%= f.hidden_field(:layer, value: layer) %>
  <%= f.submit "+", :class => "button", :id => "add_layer_#{layer}" %>
<% end %>
